package com.app.fairfree.enums;

public enum NotificationType {
    ITEM_EXPIRED, // Item Expired
    ITEM_CLAIMED, // User claimed the Item
    CLAIM_APPROVED, // Owner approved the claim
    CLAIM_DENIED, // The owner denied your claim
    CLAIM_CANCELLED // When the claimant cancels their claim
}
